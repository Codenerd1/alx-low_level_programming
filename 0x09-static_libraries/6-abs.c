#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - A function to calculate the absolute value of an integer
 * @n: The integer parameter to be evaluated
 *
 * Return: 'c' if the integer is positive or zero, otherwise return '-c'
 */
int _abs(int n)
{
	return (abs(n));
}
