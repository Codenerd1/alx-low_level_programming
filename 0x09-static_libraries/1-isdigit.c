#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - A function to check whether a charachter is a digit
 * @c: The charachter to check
 *
 * Return: 1 if the charachter is a digit, 0 if otherwise
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
