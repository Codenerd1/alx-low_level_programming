#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _puts - A function to output a string in the standard output
 * @s: The output string
 *
 * Return: A sequence of charachters(string)
 */
void _puts(char *s)
{
	puts(s);
}
