#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _putchar - Writes a character to stdout
 * @c: The character to print
 *
 * Return: On success, the character written as an unsigned char cast to an int
 * On failure, EOF is returned and errno is set appropriately.
 */

int _putchar(char c)
{
	return (putchar(c));
}
