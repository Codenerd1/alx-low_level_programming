#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _atoi - A function that converts the first part of a string to integer value
 * @s: The string to be checked
 *
 * Return: The integer value of the first part of the string
 */
int _atoi(char *s)
{
	return (atoi(s));
}
